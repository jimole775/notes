问：为什么，既然有了抽象类，还要使用接口类型呢？
答：首先，接口可以多实现，而类不可以多继承；
    虽然类也可以通过多个内部类的形式实现多继承，但是，这样做会使结构变得不清晰。
    单继承一路下来，父-子-孙，关系是明确的，处理完一层再到另一层；
    如果一下子就需要多继承来实现功能，那么，这应该是代码的设计上出现了什么问题
    而且从底层实现上来说，这还会使语言本身变得非常复杂，效率也会变低