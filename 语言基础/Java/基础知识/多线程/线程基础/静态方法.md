# 静态方法
``` java
Thread.getAllStackTraces(); // 返回所有栈信息
Thread.dumpStack(); // 返回堆栈快照
Thread.sleep(millis); // 线程睡眠
Thread.join(); // 线程加入
Thread.yield(); // 线程让步，类似于让出队列位置
Thread.setDeamon(true); // 设置后台线程
```