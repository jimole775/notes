1. 接口里面声明的方法都自动是public，所以在接口中声明的方法不需要public修饰符
2. 接口之间可以互相继承
3. 接口不可以实例化，但是可以被赋值给变量
4. 接口内部不可以定义静态方法，也不可以声明变量，但是可以声明公共静态常量，接口被实现时，常量可以直接使用，但是为了方便阅读，最好将其视为静态常量来调用，否则会很莫名其妙
5. instanceof判断实例的原型类和判断实例的原型接口，结果都是一样的
