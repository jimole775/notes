1. `private` 私有属性，不可被实例对象访问，不可被继承对象访问

2. `peotected` 保护属性，不可被实例对象访问，可以被继承对象访问

3. `public` 公共属性，指向 prototye 对象，不写描述符，默认就是public

4. `static` 静态属性

5. `readonly` 只读属性

6. `get` 定义属性的访问逻辑，一般结合 `private` 使用

7. `set` 定义属性的赋值逻辑，一般结合 `private` 使用
