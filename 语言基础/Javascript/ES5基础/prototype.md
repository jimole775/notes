实例对象会【继承】构造函数的prototype对象，

需要注意，构造函数里面的this对象，在构造函数在实例化一个对象的过程中，this是直接指向实例对象的（相当于，谁调用就是谁），

在实例对象调用属性[成员]时，如果没有找到，才会去继承的prototype对象里面寻找（也就是说，在继承prototype对象时，不会覆盖同名属性[成员]

所谓原型链就是 js里面所有创建的对象都会有一个 __proto__ 属性，指向它的原型，直到Object，之后就是null，这样就形成了一个原型链