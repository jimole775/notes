1，每个程序都必须带有一个带有main方法的类？或许吧
2，每一个对象的引用的大小都是相同的，不同的虚拟机除外
3，对引用变量进行运算时什么意思你?
4，final关键字修饰过的引用变量，将无法被赋值给另外的类？
5，某个对象的唯一引用？
6，每new一次，就会创建一个对象，如果不想再次创建对象，就直接把“对象引用”赋值给新的变量
7，数组也是对象，即使他装载的全部都是主数据类型！！！
8，二维数组 int [][] arr = {1,{2,3,4},5}