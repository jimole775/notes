public //允许所有环境下访问
private	//只有本地类的成员可以访问
default	//默认，就是不写修饰符，那么只有在相同的包，或者类里面进行访问
protected	//类成员，包成员和子类成员都可以访问，但是不同的包的其他成员不可以访问
final //修饰常量

=======================
static ：
1，静态可以修饰，变量、常量、方法、类、代码块、加载的包...
2，只加载一次
3，加载优先级高于非静态域
4, 在修饰类的时候，只允许修饰内部类
