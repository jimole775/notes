byte 1字节的长度 -8^2~8^2-1
short 2字节 -16^2~16^2-1
int 4字节 -32^2~32^2-1
long 8字节 -64^2~64^2-1

float 4字节 float a = 1.31415926F #如果后缀不加F，默认都是double精度的小数（这是根据书上的记载），那么既然有float指令来修饰a变量，还需要添加F后缀？或者说，在什么情况下，才会出现带F后缀的数值？
double 8字节 double b = 1.31415926D #双精度小数，精度是float的两倍，当然处理速度是float的1/2，一般情况下，float的精度只有7位（后面第八位被四舍五入），所以，通常都用double来存储小数，除非是有计算速度需求的场景


一般来说，关键字是小写开头的，表示的都是基础数据类型（同一个对象实例？）
但是，如果是Float这种关键字修饰的变量，一般表示的都是引用类型

Float a = 1.23F;
Float b = 1.23F;

a != b;