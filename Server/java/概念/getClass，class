两个API调用的结果是一样的；
只不过getclass是实例方法；
class是静态属性；

调用之后返回的是Class类型的对象，可以用getName方法来转成String类型

    String a = new String("a");
    Class b =  a.getClass();
    String c = b.getName();

    System.out.println(String.class.getName() instanceof String);//true