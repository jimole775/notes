Enum.valueOf(Enum.class,constant); // 返回指定的常量

[instance].class; //返回当前类，包括包的信息都有

[instance].values(); //返回所有常量，以数组的形式

[instance].ordinal(constant);//返回指定常量的下标

[instance].toString();//转成字符串类型

[instance].compareTo(otherinstance);//相当于对两个实例下标进行相减运算，最后返回int

