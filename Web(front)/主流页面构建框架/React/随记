1，组件类必须首字母大写,且组件类只能有一个顶层标签
var HelloMessage = React.createClass({

    // 每个组件类都会有一个render方法
    render: function(){
        return  <h1> Hello {this.props.name}</h1>
    }
});

// render方法相当于最后的出口了
ReactDom.render(
    <HelloMessage name="John">
        <span>Hello</span>
        <span>World</span>  // 这里的子节点可以使用this.props.children来访问
    </HelloMessage>
    document.getElementById('example'); // 这行是声明注入的目标的
)

2，访问子节点

var NodeList = React.createClass({
    render:function(){
        return (
            <ol>
                React.children.map(this.props.children, function(child){
                    return <li>child</li>
                })
            </ol>
        )
    }
})

ReactDom.render(
    <NodeList>
        <span>Hello</span>
        <span>World</span>
    </NodeList>
    document.body
);

3，使用ref属性来访问已渲染的dom