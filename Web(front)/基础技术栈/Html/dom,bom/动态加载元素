动态加载元素有两种方法:

1,document.createElement();
document.body.appendChild();
这种方法虽然麻烦,但是很安全,会持续渲染界面,并且会尝试初始化元素内的脚本;

2,document.body.innerHTML += "<div></div>"
这种方法很方便,但是需要注意的是:通过这种方法添加的元素,只会进行dom渲染,会忽略内部脚本!

***需要注意的是:以上两种方法都会使同级下的jquery绑定事件失效!
	        所以,当使用以上两种方法创建元素时,必须注意区分元素层级;