...新数据类型，它类似数组，亮点就是所有成员的值都是【唯一】的，重复的项会自动忽略。

...数据类型是对象，但是支持遍历操作。

...实例化一个新对象时，如需带参数，必须是一个数组（一般也可以用这种方式给数组去重）；

________________
   var foo = new Set([1,1,"2",{},{},[],function(){}]);

   foo.forEach(x => console.log(x));

   //1, "2", Object, Object, [], function(){}	