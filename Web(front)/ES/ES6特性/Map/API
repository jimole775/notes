===============================|set(),get()|==============================
...Map的键名可以是任意的类型，包括undefined,
...除了set可以逐一给Map添加成员，也可以在new关键字的时候，一次性添加；
______________________________________
   var map = new Map([["a",1],["b",2],["c",3]]);    //格式必须为二维数组
   map.get("a");    //1


===============================|has(),delete(),clear()|==============================
...作用看名字


===============================|keys(),values(),entries(),forEach()|==============================
...和Set对象一样