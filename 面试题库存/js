

1,重复定义一个局部变量和定义一个外部变量，他们之间的利弊

A，如果这个局部变量会重复被创建，就会多次申请内存资源，没什么必要；
B，不过外部变量，甚至是全局变量，会造成这个变量会辐射到全局错觉，对于维护一个比较大的域，会比较麻烦

，其实，如果程序的这个部分不是很吃资源的话，建议还是写到局部，如果必须写到外部或者全局，建议还是使

用get和set方法来调用和修改，否则维护起来会非常糟糕，因为js里面，每个对象都是泛类型，如果中间有一部

操作是把这个全局对象赋给其他对象之后，逻辑上就会丢失，之后被改成什么样，甚至类型改变了都不知道！