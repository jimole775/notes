/**每个花火，火焰所需要用到的点元素*/
class Diot {
    constructor(pencil, originPosition, originSize, direction, lifeTime, interval) {
        // this.position();
        this.init();

        this.draw();
    }

    init() {

    }

    position() {// 动态位置

    }

    moveDirection() {// 动态方向

    }

    color() {// 动态颜色

    }

    size() { // 动态大小


    }

    life() {// 动态生命

    }

    draw() { 
        pencil.startPath();                     
    }
}


export default Diot;